import { AiOutlineClose } from 'react-icons/ai';
import styled from 'styled-components';

export const ConviTabCloseButton = styled(AiOutlineClose)`
	margin-left: auto;
	margin-bottom: auto;
	margin-top: auto;
	:hover {
		color: red;
	}
`;
