import styled from 'styled-components';
import tw from 'twin.macro';

export const ConviModalTitlebar = styled.div`
	${tw`flex flex-row border-b-2 border-gray-50 border-solid font-bold text-lg mb-2`}
`;
